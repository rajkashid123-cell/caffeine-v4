// WorkspaceHeader is intentionally empty — the top bar was confirmed redundant.
// Mode identity is owned by the sidebar; no header chrome is shown above the workspace.
export function WorkspaceHeader() {
  return null;
}
